/**
 * payment service
 */

interface PaymentService {
  // Service methods can be added here if needed
  // For now, the controller handles everything directly
}

export default (): PaymentService => ({
  // Service methods can be added here if needed
  // For now, the controller handles everything directly
});