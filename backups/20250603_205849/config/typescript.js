module.exports = ({ env }) => ({
  /**
   * Enable TypeScript compilation for the project
   */
  enabled: true,
  
  /**
   * Generate TypeScript definitions for project schemas
   */
  autogenerate: true,
});