/**
 * artists-work router
 * Default CRUD routes for artwork API endpoints
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::artists-work.artists-work');