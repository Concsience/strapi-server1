/**
 * ordered-item router
 * Default CRUD routes for ordered item API endpoints
 */
const { factories } = require('@strapi/strapi');
module.exports = factories.createCoreRouter('api::ordered-item.ordered-item');
