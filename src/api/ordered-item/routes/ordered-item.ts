/**
 * ordered-item router
 * Default CRUD routes for ordered item API endpoints
 */

import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::ordered-item.ordered-item');