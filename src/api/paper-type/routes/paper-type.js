/**
 * paper-type router
 * Default CRUD routes for paper type API endpoints
 */
const { factories } = require('@strapi/strapi');
module.exports = factories.createCoreRouter('api::paper-type.paper-type');
