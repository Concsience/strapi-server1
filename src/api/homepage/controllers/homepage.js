/**
 * Homepage Controller - TypeScript version
 * Handles homepage content management
 */

const { factories  } = require('@strapi/strapi');

module.exports = factories.createCoreController('api::homepage.homepage');
