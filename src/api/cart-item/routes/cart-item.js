/**
 * cart-item router
 * Default CRUD routes for cart item API endpoints
 */
const { factories } = require('@strapi/strapi');
module.exports = factories.createCoreRouter('api::cart-item.cart-item');
